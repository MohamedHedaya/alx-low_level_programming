#include <stdio.h>
/**
 * main - main func
 * @argc: int
 * @argv: char
 * Return: int
 */
int main(__attribute__((unused)) int argc, char **argv)
{
	while (argv--)
		printf("%s\n", *argv++);
	return (0);
}
